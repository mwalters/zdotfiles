alias acs="apt search"
# alias aup="sudo apt-get update && sudo apt-get dist-upgrade && sudo apt-get autoremove"
alias aclean="sudo apt-get autoremove"
alias aup="sudo apt update && sudo apt full-upgrade && aclean"
alias ai="sudo apt install"
alias arm="sudo apt remove"

alias makedeb="dpkg --build"
